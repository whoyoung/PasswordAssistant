
// import * as types from '../actions/actionType';

const initState = {
    name:'click me',
}

export default function reducers(state = initState , action = {}) {
    switch (action.type) {
        default:
            return state;
    }

}
